/******************************
作者：李诗琪
完成时间：2018.11.23
功能：实现购物车里的增删查改
******************************/
#define _CRT_SECURE_NO_WARNINGS
#include "includes.h"

int main()
{
	//TEST
	/*CommodityInfo *head = NULL;
	char val[10] = { 0 };
	char val1[10] = { 0 };
	int num = 0;

	head = CreateList();
	PrintList(head);

	printf("请输入你要删除的商品：");
	scanf("%s", val);
	DeleteList(head, val);
	PrintList(head);

	printf("请输入你要插入的商品：");
	scanf("%s", val1);
	printf("请输入商品的数量:");
	scanf("%d", &num);
	InsertList(head, val1, num);
	PrintList(head);

	SortList(head);
	PrintList(head);

	ConserveList(head);

	DestroyList(head);
	head = NULL;*/

	view();

	printf("\n");
	system("pause");
	return 0;
}
